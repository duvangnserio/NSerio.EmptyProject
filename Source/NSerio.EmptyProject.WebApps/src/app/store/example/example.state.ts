import { ExampleStateModel } from '../models/example-state.model';

const exampleState: ExampleStateModel = {
  someData: [
    {
      artifactId: 0,
      name: 'Initial Data',
    },
  ],
};

export default exampleState;
